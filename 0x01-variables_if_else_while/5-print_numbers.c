#include <stdio.h>

/**
 * main - print single digits in base 10 from 0
 *
 * Return: Always 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
